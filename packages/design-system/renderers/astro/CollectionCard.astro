---
interface Props {
  title: string;
  url: string;
  imageUrl?: string;
  imageAlt?: string;
  description?: string;
}
const { title, url, imageUrl, imageAlt = title, description } = Astro.props;
---

<a href={url} class="plt-media-card plt-collection-card">
  {imageUrl && (
    <figure class="plt-media-card__media plt-collection-card__media">
      <img src={imageUrl} alt={imageAlt} loading="lazy" />
    </figure>
  )}
  <div class="plt-media-card__body">
    <h2 class="plt-headline-sm plt-media-card__title">{title}</h2>
    {description && <p class="plt-meta plt-media-card__description">{description}</p>}
  </div>
</a>
