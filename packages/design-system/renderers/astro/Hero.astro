---
interface Props {
  heading: string;
  eyebrow?: string;
  body?: string;
  imageUrl?: string;
  imageAlt?: string;
  primaryLabel?: string;
  primaryUrl?: string;
  secondaryLabel?: string;
  secondaryUrl?: string;
}
const { heading, eyebrow, body, imageUrl, imageAlt = heading, primaryLabel, primaryUrl, secondaryLabel, secondaryUrl } = Astro.props;
---

<section class:list={["plt-hero", { "plt-hero--media": Boolean(imageUrl) }]}>
  {imageUrl && <div class="plt-hero__media"><img src={imageUrl} alt={imageAlt} /></div>}
  {imageUrl && <div class="plt-hero__overlay" aria-hidden="true"></div>}
  <div class="plt-hero__content">
    <div class="plt-hero__copy">
      {eyebrow && <span class="plt-badge">{eyebrow}</span>}
      <h1 class="plt-headline-lg">{heading}</h1>
      {body && <div class="plt-body-lg">{body}</div>}
      <div class="plt-hero__actions">
        {primaryLabel && primaryUrl && <a class="plt-button plt-button--primary" href={primaryUrl}>{primaryLabel}</a>}
        {secondaryLabel && secondaryUrl && <a class="plt-button plt-button--ghost" href={secondaryUrl}>{secondaryLabel}</a>}
      </div>
    </div>
  </div>
</section>
